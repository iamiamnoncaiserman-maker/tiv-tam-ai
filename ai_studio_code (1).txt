"use client";
import React, { useState, useEffect, useRef } from 'react';
import { Mic, Send, ShoppingCart, CheckCircle2, Trash2 } from 'lucide-react';
import { createClient } from '@supabase/supabase-js';

export default function GroceryApp() {
  const [input, setInput] = useState('');
  const [messages, setMessages] = useState([{ role: 'ai', content: 'Ready! What are we buying for Tiv Taam today?' }]);
  const [list, setList] = useState([]);
  const [isRecording, setIsRecording] = useState(false);

  // Add your logic to talk to Gemini and Supabase here
  const handleSend = async (text: string) => {
    if (!text) return;
    setMessages([...messages, { role: 'user', content: text }]);
    setInput('');
    
    // This is where the AI "Brain" logic will be triggered
    // For now, it's a placeholder to show the UI works
    const response = await fetch('/api/chat', {
      method: 'POST',
      body: JSON.stringify({ message: text }),
    });
    const data = await response.json();
    setMessages(prev => [...prev, { role: 'ai', content: data.reply }]);
    refreshList();
  };

  const refreshList = async () => {
    // Logic to pull your Supabase list
  };

  return (
    <div className="flex flex-col h-screen bg-slate-50 max-w-md mx-auto border-x">
      <header className="p-4 bg-white border-b flex justify-between items-center">
        <h1 className="text-xl font-bold text-blue-600">TivTaam AI</h1>
        <ShoppingCart className="text-slate-400" />
      </header>

      <div className="flex-1 overflow-y-auto p-4 space-y-4">
        {messages.map((m, i) => (
          <div key={i} className={`p-3 rounded-2xl max-w-[80%] ${m.role === 'user' ? 'bg-blue-500 text-white self-end ml-auto' : 'bg-white border text-slate-800'}`}>
            {m.content}
          </div>
        ))}
      </div>

      <div className="p-4 bg-white border-t space-y-4">
        <div className="flex gap-2">
          <button 
            onClick={() => setIsRecording(!isRecording)}
            className={`p-3 rounded-full ${isRecording ? 'bg-red-500 animate-pulse' : 'bg-slate-100'}`}
          >
            <Mic className={isRecording ? 'text-white' : 'text-slate-600'} />
          </button>
          <input 
            className="flex-1 border rounded-full px-4 outline-none focus:border-blue-500"
            placeholder="Talk to the AI..."
            value={input}
            onChange={(e) => setInput(e.target.value)}
            onKeyDown={(e) => e.key === 'Enter' && handleSend(input)}
          />
          <button onClick={() => handleSend(input)} className="p-3 bg-blue-600 rounded-full text-white">
            <Send size={18} />
          </button>
        </div>
        <button className="w-full py-3 bg-green-600 text-white rounded-xl font-bold flex items-center justify-center gap-2">
          <ShoppingCart size={18} /> Sync to Tiv Taam Basket
        </button>
      </div>
    </div>
  );
}